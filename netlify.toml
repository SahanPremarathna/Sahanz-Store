[build]
command = "npm install --workspaces --include-workspace-root && npm run build --workspace @sahanz/web"
publish = "apps/web/dist"

[build.environment]
NODE_VERSION = "20"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
